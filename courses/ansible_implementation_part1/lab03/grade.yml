- import_playbook: section1.yml
- import_playbook: section2.yml

- hosts:
    - localhost
  connection: local
  gather_facts: no
  tasks:
    - name: Grade status
      debug:
        msg: "Lab 02 grade Section {{item.0}} is: {{item.1}}"
      with_indexed_items:
        - "{{ grade_temp_section1_1 }}"
        - "{{ grade_temp_section2_1 }}"
#        - "{{ grade_temp_section2_2 }}"
        

    - name: Grade reason
      debug:
        msg: "{{item}}"
      loop:
        - "{{ grade_reason_section1_1 }}"
        - "{{ grade_reason_section2_1 }}"
#        - "{{ grade_reason_section2_2 }}"

#
# grade:
#   lab1:
#     section1: pass
#     section2: fail
#   lab2:
#     section1: pass
#     section2: fail
#   lab3:
#     section1: pass
#     section2: fail
