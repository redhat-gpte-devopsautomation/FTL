---
- name: Print out the Service and Project we are checking
  debug:
    msg: "Checking if Service {{ service_name }} exists in project {{ project_name }}"

- name: Look up Service
  k8s_facts:
    kind: Service
    namespace: "{{ project_name }}"
    name: "{{ service_name }}"
  register: r_service

- name: Check that the Service exists
  debug:
    msg: "Success"
  when:
  - r_service.resources|length == 1
  - r_service.resources[0].metadata.name is match( service_name)
- name: Fail if the Service does not exist
  fail:
    msg: "Fail: Service {{ service_name }} not found in project {{ project_name }}"
  when: not r_service.resources|length == 1

######################################################
#                                                    #
# Common boiler plate logging logic - Do not modify  #
#                                                    #
######################################################

- name: Create logging message fact
  set_fact:
    grader_output_message: "{{ inventory_hostname + ', '\
    + task_description_message }}"
  delegate_to: localhost

- when: r_route_namespace.resources[0].spec.host != route_name
  name: Import logging tasks
  import_role:
    name: ftl_run_log_grade_to_log
...
